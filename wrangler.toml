# Workers Static Assets — Cloudflare Git builds run `npx wrangler deploy`.
# `pages_build_output_dir` is Pages-only and makes that command fail.
name = "omar-mech"
compatibility_date = "2026-08-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
